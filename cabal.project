-- !!! ATTN: file autogenerated from cabal.project.mustache on toplevel !!!
packages:
  ./pa-prelude
  ./pa-label
  ./pa-error-tree
  ./pa-field-parser
  ./pa-json
  ./pa-pretty

-- These options should only be for local development with cabal, they will not be picked up by the nix build, on CI and in releases.
package *
  -- This causes cabal to build modules on all cores, instead of just one.
  ghc-options: -j

